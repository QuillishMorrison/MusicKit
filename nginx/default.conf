server {
    listen 80;
    server_name _;

    client_max_body_size 64m;

    location = / {
        root /usr/share/nginx/html;
        try_files /index.html =404;
    }

    location = /import {
        return 301 /import/;
    }

    location /import/ {
        root /usr/share/nginx/html;
        try_files /import.html =404;
    }

    location /assets/ {
        root /usr/share/nginx/html;
        try_files $uri =404;
        expires 1h;
        add_header Cache-Control "public";
    }

    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /_auth/navidrome {
        internal;
        proxy_pass http://backend:8000/navidrome/auth;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        proxy_set_header Host $host;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header Cookie $http_cookie;
    }

    location /player/ {
        auth_request /_auth/navidrome;
        auth_request_set $navidrome_user $upstream_http_x_navidrome_user;
        error_page 401 =302 /;
        error_page 403 =302 /;
        proxy_pass http://navidrome:4533/player/;
        proxy_http_version 1.1;
        proxy_set_header Accept-Encoding "";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Prefix /player;
        proxy_set_header Remote-User $navidrome_user;
        sub_filter_once off;
        sub_filter_types text/html;
        sub_filter '</body>' '<a href="/import/" style="position:fixed;right:16px;bottom:16px;z-index:99999;background:#d66b2d;color:#fff;text-decoration:none;font:600 14px/1.2 Segoe UI,Tahoma,sans-serif;padding:12px 16px;border-radius:999px;box-shadow:0 10px 30px rgba(0,0,0,.2)">Импорт</a></body>';
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
